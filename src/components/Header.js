import React from 'react';

const Header = (props) => {

    return (
        <div>
            <header>
                <h1>Lambda Eats</h1>
                <nav>
                    <a href="#">Home</a>
                    <a href="#">Help</a>
                </nav>
            </header>
        </div>
    )
}

export default Header;